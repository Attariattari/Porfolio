// domMax preserves the existing layout, gesture, exit, and transform
// animations. It is loaded asynchronously by LazyMotion instead of being
// embedded repeatedly in public page entry bundles.
export { domMax as default } from "framer-motion";
